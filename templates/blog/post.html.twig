{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
		<p>ID {{ id }}</p>
		<h3>{{ post.title }}</h3>
		<p>{{ post.text }}</p>
		<p>{{ post.date|date('Y-m-d H:i:s') }}</p>
		<p>{{ 78697869|price }}</p>
		<p>here is the curent locale {{ locale }}</p>
{% endblock %}